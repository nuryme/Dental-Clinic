{
  "name": "smile-dental",
  "private": true,
  "version": "1.0.0",
  "description": "Smile Dental — full-stack clinic app (React SPA + Express API on one Vercel project)",
  "scripts": {
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "mongodb": "^6.12.0",
    "morgan": "^1.10.0"
  }
}
